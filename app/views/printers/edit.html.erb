<section>
  <div class="content-wrapper">
    <div class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <h2 class="m-0 text-dark">EDITAR</h2>

<%= render 'form', printer: @printer %>
<%= link_to 'Back', printers_path %>
</section>