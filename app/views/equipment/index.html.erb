      <%= render "layouts/admin_nav" %>
      <%= render "layouts/admin_side_nav" %>
<div class="content-wrapper">
  <div class="content-header">
    <div class="container-fluid">
      <div class="row mb-2">
        <div class="col-sm-6">
          <h1 class="m-0 text-dark">EQUIPOS INFORMATICOS </h1>
        </div>
      </div>
      <p id="notice"><%= notice %></p>
      <section>
        <div class="card">
          <div class="card-body">
            <p align="right"><%= link_to 'Agregar +' , new_equipment_path, class: "btn btn-success btn-sm" %></p>
            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th scope="col">Rotulado</th>
                    <th scope="col">Placa Madre</th>
                    <th scope="col">Sistema Operativo</th>
                    <th scope="col">Impresora</th>
                    <th scope="col">Opción</th>
                  </tr>
                </thead>
                <tbody>
                  <% @equipment.each do |equipment| %>
                    <tr>
                      <td><%= equipment.labelled %></td>
                      <td><%= equipment.motherboard %></td>
                      <td><%= equipment.so %></td>
                      <td><%= equipment.print_machine %></td>
                      <td><%= link_to 'Ver', equipment, class: "btn btn-outline-success btn-sm" %>
                        <%= link_to 'Editar', edit_equipment_path(equipment), class: "btn btn-outline-primary btn-sm" %>
                        <%= link_to 'Eliminar', equipment, method: :delete, data: { confirm: 'Estas seguro?..' }, class: "btn btn-outline-danger btn-sm" %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </section>
    </div>
  </div>
</div>
</div>
